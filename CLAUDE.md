# CLAUDE.md

Claude-specific bootstrap file for `~/Development/trmnl-plugins`.

Primary instructions live in `@AGENTS.md` (source of truth).

Use targeted imports for active plugin work:
- `@plugins/<plugin>/README.md`
- `@plugins/<plugin>/src/settings.yml`
- `@plugins/<plugin>/src/shared.liquid`

Keep this file lean; shared rules belong in `AGENTS.md`.
